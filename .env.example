# Monorepo root envs (optional for local tooling)
# Add shared values here and copy into app-specific env files as needed.

# --- Web (Next.js) ---
# Base URL of your deployed API (Railway/Render/etc.)
# Example: https://fitsync-api.up.railway.app
NEXT_PUBLIC_API_URL=

# Disable Next telemetry if desired
NEXT_TELEMETRY_DISABLED=1

# --- Database (Prisma/Neon) ---
# Primary connection string (Neon recommended; include sslmode=require)
DATABASE_URL=
# Optional direct connection string for migrations (bypass pooler)
DIRECT_DATABASE_URL=
